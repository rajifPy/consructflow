{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm build --filter=equip-app",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
