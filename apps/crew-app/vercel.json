{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && pnpm build --filter=crew-app",
  "installCommand": "npm install -g pnpm@9.1.0 && pnpm install --no-frozen-lockfile",
  "outputDirectory": ".next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ../../apps/crew-app ../../packages"
}
